import styled from "styled-components";
import { FlexCenterStyle } from "@S/style/responsive/display";

export const SingleChatLabel = styled.div`
  width: 4rem;
  height: 6.3rem;
  align-items: row;
  ${FlexCenterStyle};
  position: fixed;
  z-index: 10;
`;
